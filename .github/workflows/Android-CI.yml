name: CI

on: [pull_request]

jobs:
  Build:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: 11
      - name: Build project
        run: ./gradlew assembleDebug
      - name: Run tests
        run: ./gradlew test

